{
  "name": "rule-helper",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "client-start": "cd client && yarn client-start",
    "client-build": "cd client && yarn client-build",
    "client-test": "cd client && yarn client-test",
    "client-eject": "cd client && yarn client-eject",
    "server-start": "cd server && yarn-server-start"
  }
}
